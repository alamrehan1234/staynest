<% layout("/layouts/boilerplate")%>

    <body>
        <h2>Edit Listing</h2>

        <!-- <form method="POST" action="/listings/<%=listing._id %>?_method=PUT">
            <input name="listings[title]" placeholder="enter title" value="<%=listing.title %>" type="text" required>
            <br>
            <br>
            <textarea name="listings[description]" placeholder="enter description"
                required><%=listing.description%></textarea>
            <br>
            <br>
            <textarea name="listings[image.url]" placeholder="enter image url"
                type="text"><%=listing.image.url %></textarea>
            <br>
            <br>
            <input name="listings[price]" placeholder="enter price" value="<%=listing.price%>" type="number" required>
            <br>
            <br>
            <input name="listings[location]" placeholder="enter location" value="<%=listing.location%>" type="text"
                required>
            <br>
            <br>
            <input name="listings[country]" placeholder="enter country" value="<%=listing.country%>" type="text"
                required>
            <br>
            <br>
            <button>Update</button>
            <br>
        </form> -->

        <form method="POST" action="/listings/<%=listing._id %>?_method=PUT">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input name="listings[title]" class="form-control" value="<%=listing.title %>" placeholder="enter title"
                    type="text" required>
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Description</label>
                <textarea name="listings[description]" class="form-control" placeholder="enter description"
                    required><%=listing.description %></textarea>
            </div>
            <div class="mb-3">
                <label for="image" class="form-label">
                    Image URL
                </label>
                <input name="listings[image][filename]" value="listingimage" type="hidden">
                <textarea name="listings[image][url]" class="form-control" placeholder="enter image url"
                    type="text"><%=listing.image.url %></textarea>
            </div>
            <div class="mb-3">
                <label for="price" class="form-label">Price</label>
                <input name="listings[price]" class="form-control" value="<%=listing.price %>" placeholder="enter price"
                    type="number" required>
            </div>
            <div class="mb-3">
                <label for="location" class="form-label">Location</label>
                <input name="listings[location]" class="form-control" value="<%=listing.location %>"
                    placeholder="enter location" type="text" required>
            </div>
            <div class="mb-3">
                <label for="country" class="form-label">Country</label>
                <input name="listings[country]" class="form-control" value="<%=listing.country %>"
                    placeholder="enter country" type="text" required>
            </div>
            <button class="btn btn-dark">UPDATE</button>
            <br>
        </form>
    </body>